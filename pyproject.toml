[project]
name = "sql"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    'flask',
    "flask-sqlalchemy",
    'flask-Login',
    "flask-WTF",
    'email_validator',
    "psycopg2-binary",
    "flask-migrate",
]
